import { useState } from 'react';import { uid } from '../utils/attendance';
const days=['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'];
export default function Timetable({data,setData}){const[f,setF]=useState({day:'Monday',subjectId:'',start:'09:00',end:'10:00',room:''});const add=e=>{e.preventDefault();const s=data.subjects.find(x=>x.id===f.subjectId);if(!s)return;setData(d=>({...d,timetable:[...d.timetable,{...f,id:uid(),subjectName:s.name}]}))};return <main><h1>Timetable</h1><form className="card form" onSubmit={add}><select value={f.day} onChange={e=>setF({...f,day:e.target.value})}>{days.map(d=><option key={d}>{d}</option>)}</select><select required value={f.subjectId} onChange={e=>setF({...f,subjectId:e.target.value})}><option value="">Select subject</option>{data.subjects.map(s=><option value={s.id} key={s.id}>{s.name}</option>)}</select><div className="row"><input type="time" value={f.start} onChange={e=>setF({...f,start:e.target.value})}/><input type="time" value={f.end} onChange={e=>setF({...f,end:e.target.value})}/></div><input placeholder="Room number (optional)" value={f.room} onChange={e=>setF({...f,room:e.target.value})}/><button className="primary">Add Class</button></form>{days.map(day=><section key={day}><h2>{day}</h2>{data.timetable.filter(x=>x.day===day).map(c=><div className="card mini" key={c.id}><span><b>{c.subjectName}</b> {c.start}–{c.end} {c.room&&`· ${c.room}`}</span><button className="dangerText" onClick={()=>setData(d=>({...d,timetable:d.timetable.filter(x=>x.id!==c.id)}))}>Delete</button></div>)}</section>)}</main>}
