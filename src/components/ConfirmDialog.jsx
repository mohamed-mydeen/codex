export default function ConfirmDialog({ open, title, message, confirmText='Confirm', onConfirm, onCancel }) { if (!open) return null; return <div className="modalBackdrop"><div className="modal"><h3>{title}</h3><p>{message}</p><div className="row end"><button onClick={onCancel}>Cancel</button><button className="danger" onClick={onConfirm}>{confirmText}</button></div></div></div>; }
